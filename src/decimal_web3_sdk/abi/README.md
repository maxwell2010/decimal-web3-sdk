ABI files will live here.

For now, legacy ABI files are preserved under:

`backup/legacy_*/sdk_release/abi`

Before implementing transaction builders, copy only verified ABI files from the legacy backup or regenerate them from `go-smart-node/contracts`.

