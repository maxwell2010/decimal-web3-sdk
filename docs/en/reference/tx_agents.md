# tx_agents

[Index](../api.md)

Signatures are generated from the release source. Required fields have no default.

## BuildNativeTransferAgent

Buildnativetransferagent; types, defaults and return value are specified below.

### run

Run; types, defaults and return value are specified below.

```python
async run(self, context: 'AgentContext') -> 'AgentResult'
```

## EstimateGasAgent

Estimategasagent; types, defaults and return value are specified below.

### run

Run; types, defaults and return value are specified below.

```python
async run(self, context: 'AgentContext') -> 'AgentResult'
```

## SignTransactionAgent

Signtransactionagent; types, defaults and return value are specified below.

### run

Run; types, defaults and return value are specified below.

```python
async run(self, context: 'AgentContext') -> 'AgentResult'
```

## BroadcastTransactionAgent

Broadcasttransactionagent; types, defaults and return value are specified below.

### run

Run; types, defaults and return value are specified below.

```python
async run(self, context: 'AgentContext') -> 'AgentResult'
```

## ReceiptPollAgent

Receiptpollagent; types, defaults and return value are specified below.

### run

Run; types, defaults and return value are specified below.

```python
async run(self, context: 'AgentContext') -> 'AgentResult'
```
