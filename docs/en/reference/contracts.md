# contracts

[Index](../api.md)

Signatures are generated from the release source. Required fields have no default.

## AbiRegistry

Abiregistry; types, defaults and return value are specified below.

### load

Load; types, defaults and return value are specified below.

```python
load(self, name: 'str') -> 'list[dict[str, Any]]'
```
